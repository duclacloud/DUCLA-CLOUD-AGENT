agent:
  id: "${HOSTNAME}"
  name: "${HOSTNAME}"
  environment: "production"

master:
  url: "${DUCLA_MASTER_URL}"
  token: "${DUCLA_AGENT_TOKEN}"

api:
  http:
    enabled: true
    port: 8080
  grpc:
    enabled: true
    port: 8443

logging:
  level: "info"
  format: "json"
  output: "stdout"
